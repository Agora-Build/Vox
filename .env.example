# =============================================================================
# Vox Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values for your environment.
# Required variables are marked with [REQUIRED], optional with [OPTIONAL].
# =============================================================================

# -----------------------------------------------------------------------------
# Core Settings [REQUIRED]
# -----------------------------------------------------------------------------

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://vox:vox123@localhost:5432/vox

# Session encryption secret - use a long random string in production
# Generate with: openssl rand -hex 32
SESSION_SECRET=your-session-secret-change-in-production

# System initialization code - required for first-time setup via /api/auth/init
# This code is used to create the initial admin user
INIT_CODE=your-init-code-here

# -----------------------------------------------------------------------------
# Server Settings [OPTIONAL]
# -----------------------------------------------------------------------------

# Server port (default: 5000)
PORT=5000

# Application URL - used for OAuth callbacks and email links
# In production, set this to your public URL
APP_URL=http://localhost:5000

# Node environment (development, staging, production)
NODE_ENV=development

# -----------------------------------------------------------------------------
# Google OAuth [OPTIONAL]
# -----------------------------------------------------------------------------
# Enable Google sign-in by providing these credentials.
# Create OAuth 2.0 credentials at: https://console.cloud.google.com/apis/credentials

# Google OAuth Client ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google OAuth Callback URL (default: /api/auth/google/callback)
# In production, use full URL: https://yourdomain.com/api/auth/google/callback
GOOGLE_CALLBACK_URL=/api/auth/google/callback

# -----------------------------------------------------------------------------
# Stripe Payments [OPTIONAL]
# -----------------------------------------------------------------------------
# Enable Stripe payments for organization seat purchases.
# Get your keys at: https://dashboard.stripe.com/apikeys

# Stripe Secret Key (starts with sk_live_ or sk_test_)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key

# Stripe Publishable Key (starts with pk_live_ or pk_test_)
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# Stripe Webhook Secret (starts with whsec_)
# Get this when setting up webhooks at: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret

